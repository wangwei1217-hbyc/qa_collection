<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/task
       http://www.springframework.org/schema/task/spring-task.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">


    <!-- 早上9点和下午2点分别处理一次-->
   <!-- <task:scheduled-tasks>
        <task:scheduled ref="pullDriverInfosTask" method="pullDriverInfos" cron="0 0 9,14 * * ? "/>
        <task:scheduled ref="pullDriverInfosTask" method="remindLogoutRecovery" cron="0 0 9,14 * * ? "/>
        <task:scheduled ref="pullDriverInfosTask" method="remindOverage" cron="0 0 9,14 * * ? "/>
        <task:scheduled ref="pullDriverInfosTask" method="remindSuperScore" cron="0 0 9,14 * * ? "/>
    </task:scheduled-tasks>

    <task:scheduled-tasks>
        <task:scheduled ref="syncFeesCostTask" method="syncFees" cron="0 0 0 * * ?"/>
    </task:scheduled-tasks>

    <task:scheduled-tasks>
        <task:scheduled ref="pullDriverInfosTask" method="pullForMedicalTest" cron="0 0/5 7-20 * * ? "/>
        <task:scheduled ref="pullDriverInfosTask" method="pullForAudited" cron="0 0/15 7-20 * * ? "/>
    </task:scheduled-tasks>-->

</beans>